Alembic migrations for the Sistema do Cartório.

Common commands (executed from repo root):

    alembic revision --autogenerate -m "describe change"
    alembic upgrade head
    alembic downgrade -1
    alembic history
